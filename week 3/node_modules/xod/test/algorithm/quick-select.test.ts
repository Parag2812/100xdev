import { rand, quickSelect } from '../../src';

describe.only('Test Quick Select Function.', () => {
  test('Quick Select(1)(2)', () => {
    const n = 100;
    const d = rand.intArray(n);

    for (let i = 0; i < n; ++i) {
      expect(quickSelect(d.slice(), i + 1)).toBe(i);
    }
  });
});
