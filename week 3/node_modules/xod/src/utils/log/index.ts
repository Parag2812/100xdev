import { ILogData, ILogOptions } from './type';
import { parse } from './parse';

export function log(data: ILogData, options: ILogOptions = {}) {
  const logInfo = parse(data, options);
  console.log(logInfo.v.join('\n'));
}
